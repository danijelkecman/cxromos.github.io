# Repository Writing Rules

## Slop guardrails

- Do not end bullet points with `;` or `.`
- Use `-` instead of an em dash
- Apply these rules to site copy, Markdown, documentation, code comments, and metadata
